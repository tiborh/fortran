      program testingInt
      implicit none

      integer :: largeval
!     huge() gives the largest value that can be held by the variable
      print *, huge(largeval)
   
      end program testingInt
